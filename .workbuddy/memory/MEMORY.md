# 项目级记忆（MEMORY.md）

本文件是 `myOnlineProject` 的**长期事实源**。任何 AI 在本工作区执行需求时，都必须先遵循以下约定。

## 设计语言（来自 agent.md）
- 配色：`--bg #f3efe6`（暖米底） / `--ink #1c1a16`（墨色字） / `--accent #9a5b34`（陶土强调）
- 字体：标题 `Cormorant Garamond`（衬线），正文 `Space Grotesk`（无衬线）
- 调性：高级简约、留白充足、动效从简，禁止霓虹/高饱和撞色与 emoji 堆砌

## 技术栈（固定）
- Vue 3（`<script setup>`） + Vite + TypeScript（strict） + Pinia + Vue Router

## ⚠️ 执行前必读
> 每次收到本项目的新需求时，AI **必须先读取并严格遵循**
> `E:\my_projects\myOnlineProject\agent.md`
> 的设计系统与技术栈，不得擅自引入冲突的视觉或技术风格。

完整规范见 `agent.md`（项目根目录）。
